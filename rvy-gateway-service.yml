server:
  port: 8999


eureka:
  instance:
    hostname: localhost

spring:
  cloud:
    gateway:
      routes:      
      - id: rvy-product-service
        uri: http://localhost:8081/
        predicates:
          - Path=/rvy/api/**     
